#if 0


#include <iostream>
#include <stdio.h>
#include <conio.h>

using namespace std;

// Exception handling - 
    //    (1) Try block- in which area you want to check
    //    (2) Throw - - throw a number for error 
    //    (3)  Catch - catch the error number showing by throw - catch block right after try block
    //    (4)  catch(...) - 3 dots inside the brackets means it can handle any data type

/*
int main()
{
   int a = 43;
   int b = 41;

   try{

     if (a>b)
       throw 99;
    }
   catch(int x)  // or catch(...)  ... 3 dots means it can handle any data from throw 
   {
     cout<< "a can't be greater than b" << x << endl;
    }

   getch();
   return 0;
}

*/


#endif

